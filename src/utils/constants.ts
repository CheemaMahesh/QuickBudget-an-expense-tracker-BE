
const invalidSignUpDetails: string = "name: it should be a string with the length between 1-100, email: it should be in string with the legth between: 5-100 as email, password: it should be a string with legth between 6-100";
const invalidSignInDetails: string = "email: it should be in string with the legth between: 5-100 as email, password: it should be a string with legth between 6-100";

const invalidFormat: string = "Format is not valid";

const invalidUserLogin: string = "Email or Password are incorrect!";

export {
    invalidSignUpDetails,
    invalidFormat,
    invalidSignInDetails,
    invalidUserLogin,
}